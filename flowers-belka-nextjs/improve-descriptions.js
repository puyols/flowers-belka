// Скрипт для улучшения описаний букетов
const fs = require('fs');

// База знаний о цветах и их символике
const flowerInfo = {
  'Роза Эквадор': {
    description: 'премиальные розы Эквадор с крупными бутонами и насыщенным ароматом',
    symbolism: 'символ страстной любви и глубоких чувств',
    features: 'долго сохраняют свежесть, обладают изысканным ароматом'
  },
  'Роза': {
    description: 'классические розы с нежными лепестками',
    symbolism: 'символ любви, красоты и совершенства',
    features: 'универсальный подарок для любого случая'
  },
  'Пион': {
    description: 'пышные пионы с многослойными лепестками',
    symbolism: 'символ богатства, процветания и счастья',
    features: 'сезонные цветы (май-июль), обладают нежным ароматом'
  },
  'Тюльпан': {
    description: 'весенние тюльпаны с изящными бутонами',
    symbolism: 'символ весны, нежности и обновления',
    features: 'яркие весенние цветы, символ пробуждения природы'
  },
  'Гипсофила': {
    description: 'воздушная гипсофила с мелкими белоснежными цветочками',
    symbolism: 'символ чистоты, невинности и легкости',
    features: 'создает эффект воздушного облака, долго сохраняет вид'
  },
  'Хризантема': {
    description: 'яркие хризантемы с пышными соцветиями',
    symbolism: 'символ долголетия, радости и оптимизма',
    features: 'осенние цветы, отличаются особой стойкостью'
  },
  'Гвоздика': {
    description: 'изящные гвоздики с резными лепестками',
    symbolism: 'символ восхищения и глубокой привязанности',
    features: 'обладают пряным ароматом, долго стоят в вазе'
  },
  'Эустома': {
    description: 'нежные эустомы с шелковистыми лепестками',
    symbolism: 'символ благодарности и восхищения',
    features: 'напоминают розы, но более нежные и воздушные'
  },
  'Альстромерия': {
    description: 'яркие альстромерии с пестрыми лепестками',
    symbolism: 'символ дружбы и преданности',
    features: 'долго сохраняют свежесть, имеют яркую окраску'
  },
  'Гортензия': {
    description: 'пышные гортензии с крупными шаровидными соцветиями',
    symbolism: 'символ искренности и изобилия',
    features: 'создают объемную текстуру в букете'
  }
};

// Функция для генерации описания на основе тегов
function generateDescription(name, tags, category) {
  const flowers = tags.filter(tag => flowerInfo[tag]);
  
  if (flowers.length === 0) {
    return generateGenericDescription(name, category);
  }

  const mainFlower = flowers[0];
  const flowerData = flowerInfo[mainFlower];
  
  let description = `<h2>${name} – изысканная композиция для особых моментов</h2>`;
  
  if (flowers.length === 1) {
    description += `<p>Этот букет создан из ${flowerData.description}. ${flowerData.symbolism.charAt(0).toUpperCase() + flowerData.symbolism.slice(1)}, этот букет станет прекрасным подарком для выражения ваших чувств.</p>`;
  } else {
    description += `<p>Изысканная композиция, сочетающая в себе ${flowers.map(f => flowerInfo[f].description).join(', ')}. Каждый цветок в этом букете несет свою символику и создает гармоничную композицию.</p>`;
  }

  description += `<h3>Состав букета:</h3><ul>`;
  flowers.forEach(flower => {
    description += `<li><strong>${flower}</strong> – ${flowerInfo[flower].symbolism}</li>`;
  });
  description += `</ul>`;

  description += `<h3>Особенности:</h3><ul>`;
  flowers.forEach(flower => {
    description += `<li>${flowerInfo[flower].features}</li>`;
  });
  description += `</ul>`;

  description += `<div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 my-4">`;
  description += `<p><strong>Важно:</strong> Состав букета может варьироваться в зависимости от сезона и наличия цветов. Мы всегда стремимся создать композицию, которая превзойдет ваши ожидания.</p>`;
  description += `</div>`;

  description += `<p>Если нужных цветов не окажется в наличии, мы создадим для вас уникальный букет, учитывая ваши пожелания, и организуем доставку в кратчайшие сроки!</p>`;
  
  description += `<p><em>Подарите радость и красоту с этим изысканным букетом. Ведь цветы – это язык, понятный сердцу.</em></p>`;

  return description;
}

// Функция для генерации общего описания
function generateGenericDescription(name, category) {
  const categoryDescriptions = {
    'bukety_tsvetov': 'букет из отборных цветов',
    'rozy': 'букет из роз',
    'tulpany': 'букет из тюльпанов',
    'tsvety_v_korobke': 'композиция в коробке',
    'piony': 'букет из пионов'
  };

  const desc = categoryDescriptions[category] || 'цветочная композиция';
  
  return `<h2>${name} – изысканная композиция для особых моментов</h2><p>Этот ${desc} создан с особой заботой и вниманием к деталям. Каждый цветок тщательно отобран нашими флористами для создания идеальной композиции, которая станет прекрасным подарком для выражения ваших чувств.</p><h3>Наши гарантии:</h3><ul><li>Только свежие цветы высочайшего качества</li><li>Профессиональная флористика</li><li>Быстрая и аккуратная доставка</li><li>Индивидуальный подход к каждому заказу</li></ul><div class="bg-blue-50 border-l-4 border-blue-400 p-4 my-4"><p><strong>Важно:</strong> Состав букета может варьироваться в зависимости от сезона и наличия цветов. Мы всегда стремимся создать композицию, которая превзойдет ваши ожидания.</p></div><p>Если нужных цветов не окажется в наличии, мы создадим для вас уникальный букет, учитывая ваши пожелания, и организуем доставку в кратчайшие сроки!</p><p><em>Подарите радость и красоту с нашими букетами. Ведь цветы – это язык, понятный сердцу.</em></p>`;
}

console.log('🌸 Начинаем улучшение описаний букетов...');

// Читаем файл
const filePath = './src/data/products-parsed.ts';
let content = fs.readFileSync(filePath, 'utf8');

// Парсим продукты (упрощенный парсинг)
const productMatches = content.match(/\{[^}]*name: '[^']*'[^}]*\}/g);

if (!productMatches) {
  console.log('❌ Не удалось найти продукты в файле');
  process.exit(1);
}

console.log(`📦 Найдено ${productMatches.length} продуктов для обработки`);

// Обрабатываем каждый продукт
let updatedCount = 0;

// Простые описания, которые нужно заменить
const simpleDescriptions = [
  'Элегантный букет цветов, собранный с особой заботой. Подарит радость и улыбку вашим близким.',
  'Утонченный букет из цветов, созданный с любовью и вниманием к деталям.',
  'Роскошная композиция из цветов, которая станет незабываемым подарком для ваших близких.',
  'Нежная композиция из цветов высочайшего качества. Станет прекрасным дополнением к любому празднику.',
  'Изысканный букет из свежих цветов. Идеальный подарок для выражения ваших чувств и создания особенного настроения.',
  'Основной стиль карточки (ОБНОВЛЕНО - более утонченный и "собранный")',
  'Основной стиль карточки (ОБНОВЛЕНО - максимальная "чистота" и "легкость")'
];

// Заменяем простые описания
simpleDescriptions.forEach(simpleDesc => {
  if (content.includes(`description: '${simpleDesc}'`)) {
    // Находим контекст для генерации нового описания
    const regex = new RegExp(`name: '([^']*)'[^}]*tags: \\[([^\\]]*)\\][^}]*category: '([^']*)'[^}]*description: '${simpleDesc.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}'`, 'g');
    
    content = content.replace(regex, (match, name, tagsStr, category) => {
      const tags = tagsStr.split(',').map(t => t.trim().replace(/'/g, ''));
      const newDescription = generateDescription(name, tags, category);
      updatedCount++;
      console.log(`✅ Обновлено описание для: ${name}`);
      return match.replace(`description: '${simpleDesc}'`, `description: '${newDescription}'`);
    });
  }
});

// Записываем обновленный файл
fs.writeFileSync(filePath, content);

console.log(`🎉 Готово! Обновлено описаний: ${updatedCount}`);
console.log('📝 Все букеты теперь имеют подробные и уникальные описания!');
